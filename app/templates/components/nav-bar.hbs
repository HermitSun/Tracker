<nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button"
                    class="navbar-toggle collapsed"
                    data-toggle="collapse"
                    data-target="#top-navbar-collapse">
                <span class="sr-only">Toggle navigation</span>
                <!--三个span代表三道杠，也就是≡；同理，两个span就是两道杠-->
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand">Tracker</a>
        </div>
        <div class="collapse navbar-collapse"
             id="top-navbar-collapse">
            <ul class="nav navbar-nav">
                {{#if showSightings}}
                    <li>
                        {{#link-to 'sightings'}}Sightings{{/link-to}}
                    </li>
                {{/if}}
                {{#if showCryptids}}
                    <li>
                        {{#link-to 'cryptids'}}Cryptids{{/link-to}}
                    </li>
                {{/if}}
                {{#if showWitnesses}}
                    <li>
                        {{#link-to 'witnesses'}}Witnesses{{/link-to}}
                    </li>
                {{/if}}
                {{#if showCustom}}
                    <li>
                        {{#link-to 'witnesses'}}CustomRoute{{/link-to}}
                    </li>
                {{/if}}
            </ul>
        </div>
    </div>
</nav>